---
import { getCollection } from 'astro:content';
const walks = await getCollection('walks');
const eastWalks = walks.filter(walk => walk.data.category === 'east');
---

<BaseLayout title="East Walks" description="Explore curated walks in Amsterdam's east.">
  <main>
    <section>
      <h2>East Walks</h2>
      <div class="walks-grid">
        {walks.length === 0 ? (
          <p>No walks in this category yet.</p>
        ) : (
          walks.map(walk => (
            <WalkCard {...walk.data} href={`/walks/${walk.id}`} />
          ))
        )}
      </div>
    </section>
  </main>
</BaseLayout>
